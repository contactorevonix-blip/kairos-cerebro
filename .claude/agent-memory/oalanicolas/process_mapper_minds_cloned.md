---
name: process-mapper-minds-cloned
description: DNA clones built for the process-mapper squad agents (swim-lane / process-mapping minds), slugs, source tiers, file paths
metadata:
  type: project
---

# process-mapper squad — minds cloned

Squad `process-mapper` (entry: cartographer-chief) maps AIOX processes/agents. Each Tier-1 specialist is based on a real process-thinking mind. Agent files live at `squads/process-mapper/agents/{slug}.md`. Format is identical to the Gene Kim / Forsgren atomic-agent YAML (see `squads/deep-research/agents/forsgren.md` for the canonical template).

**Why:** Pedro is building a process-observability squad; agents must carry verifiable framework DNA, every claim tagged `[SOURCE:]` (verified) or `[INFERÊNCIA]` (AIOX application).

**How to apply:** when extending these agents, keep the same YAML block structure (persona, frameworks, commands, voice_dna, output_examples, anti_patterns, thinking_dna) and the `[SOURCE:]`/`[INFERÊNCIA]` tagging discipline. Max ~350 lines per agent.

## Cloned

- **agent-cartographer** (Geary Rummler) — fidelity high, Tier 0. Frameworks: Three Levels of Performance / Nine Performance Variables, Swim-Lane (cross-functional) diagram, Super-System map, White-Space analysis, IS/SHOULD, three process types. Source: *Improving Performance: How to Manage the White Space on the Organization Chart*, Rummler & Brache (Jossey-Bass, 1990, ISBN 0-7879-8799-X) + Handbook of Human Performance Technology (1999). Verified via strategicmanagementinsight.com nine-boxes matrix + rummlerbrache.com. Focus: swim-lanes, authority maps, delegation trees, handoffs for the 12 AIOX agents.

## squad.yaml `based_on` map (other specialists, not yet cloned by me)
- process-auditor → Gene Kim (Current State Mapping)
- flow-architect → Gene Kim (Value Stream Mapping)
- structure-mapper → Original
- evolution-tracker → Tom Gilb (Evolutionary Delivery)
- map-validator → W.E. Deming (Quality)
