# Agent System — Single Source of Truth & Drift Report

> **Story:** [10.2 — Agent System Single Source of Truth + Drift Audit](../stories/10.2.story.md) · **Epic:** EPIC-10 (Framework Foundation Cleanup) · **Findings:** F3, F4
> **Generated:** 2026-06-14 · **Maintained by:** @dev / @architect
> **Verification:** `node --test tests/agents/agent-drift-audit.test.js`

This document proves that the AIOX agent system has a **single canonical source of truth**, lists every sync target and its format, classifies all source→target divergences as **intentional** or **accidental**, and records the drift-audit result (0 accidental drift).

---

## 1. Single Source of Truth

| Property | Value |
|----------|-------|
| **Canonical source** | `.aiox-core/development/agents/` (12 `*.md` files) |
| **Config key** | `core-config.yaml → ideSync.source` |
| **Pipeline** | `.aiox-core/infrastructure/scripts/ide-sync/index.js` |
| **Drift enforcement** | `ideSync.validation.failOnDrift: true`, `strictMode: true` |

The 12 canonical agents are: `aiox-master`, `analyst`, `architect`, `data-engineer`, `dev`, `devops`, `pm`, `po`, `qa`, `sm`, `squad-creator`, `ux-design-expert`.

> The multiple `agents/` directories across `.codex`, `.antigravity`, `.claude`, `.github` are **sync targets generated from the source** — not orphan copies. Editing them directly is an anti-pattern; changes are made in the source and propagated via `ideSync`.

---

## 2. Sync Targets

Declared in `core-config.yaml → ideSync.targets`. Each target has a per-IDE **format** (transformer) — divergence in *shape* between formats is by design; only divergence in *agent content* counts as drift.

| Target | Path | Format (transformer) | Generated in repo? |
|--------|------|----------------------|--------------------|
| `claude-code` | `.claude/commands/AIOX/agents` (+ `.claude/skills`) | `full-markdown-yaml` | ✅ Yes (12 + 12 skills) |
| `codex` | `.codex/agents` | `full-markdown-yaml` | ✅ Yes (12 + 4 redirects) |
| `github-copilot` | `.github/agents` | `github-copilot` (`*.agent.md`) | ✅ Yes (12) |
| `antigravity` | `.antigravity/rules/agents` | `cursor-style` | ✅ Yes (12 + 4 redirects) |
| `gemini` | `.gemini/rules/AIOX/agents` (+ `.gemini/commands/*.toml`) | `full-markdown-yaml` | ⚠️ Not generated yet |
| `cursor` | `.cursor/rules/agents` | `condensed-rules` (`*.mdc`) | ⚠️ Not generated yet |
| `kimi` | `.kimi/skills/<id>/SKILL.md` | `kimi-skill` | ⚠️ Not generated yet |

---

## 3. Drift Report (AC1) — 2026-06-14

Produced by the existing ideSync validator (`validate --verbose`), which content-hashes each target file against the content the source would generate for that target's format.

| Target | Synced | Missing | **Drift** | Orphaned |
|--------|-------:|--------:|----------:|---------:|
| `claude-code` | 12 | 0 | **0** | 46 |
| `claude-code-skills` | 12 | 0 | **0** | 58 |
| `codex` | 12 | 0 | **0** | 4 |
| `github-copilot` | 12 | 0 | **0** | 0 |
| `antigravity` | 12 | 0 | **0** | 4 |
| `gemini` | 0 | 12 | **0** | 0 |
| `gemini-commands` | 0 | 13 | **0** | 0 |
| `cursor` | 0 | 12 | **0** | 0 |
| `kimi` | 0 | 12 | **0** | 0 |
| **TOTAL** | **60** | **49** | **0** | **112** |

> **Headline:** **0 accidental content drift.** Every target that has been generated reproduces the 12 canonical source agents byte-for-byte (after line-ending normalization).

---

## 4. Divergence Classification (AC2)

### Intentional — per-IDE format

The `Synced` counts already account for format differences (`*.agent.md` for GitHub Copilot, `*.mdc` for Cursor, `SKILL.md` subdirs for Kimi). These are produced by the source via each transformer — **not drift**.

### Intentional — not-yet-generated targets (`Missing`)

`gemini`, `gemini-commands`, `cursor`, `kimi` show `Missing` because their folders have **never been generated** for this repo. Running `ide-sync sync --ide <name>` would create them from the same source. This is expected, not accidental drift.

### Intentional — `Orphaned` files

| Orphaned set | Target(s) | Classification |
|--------------|-----------|----------------|
| 46 squad/expert agents (`aiox-cerebro`, `forge-*`, `kahneman`, `squad-chief`, …) | `.claude/commands`, `.claude/skills` | **Intentional** — these are **squad/project agents**, a *different* agent system not sourced from `.aiox-core/development/agents`. They legitimately live only in `.claude`. |
| 4 redirect stubs (`aiox-developer`, `aiox-orchestrator`, `db-sage`, `github-devops`) | `.codex`, `.antigravity` | **Intentional** — deprecated-agent **redirect stubs** generated by the ideSync redirect generator (e.g. `db-sage → @data-engineer`). Benign; tolerated by `failOnOrphaned: false`. |

> **`redirects: {}` decision:** the redirect map in `core-config.yaml` is intentionally empty. A *global* redirect map would create **false drift** in `.claude/commands` (where `db-sage.md` is a real squad agent, not a redirect) and **false "missing"** in github/gemini/cursor/kimi (which never received stubs). With an empty map, drift is 0 everywhere and the 4 codex/antigravity stubs remain as benign orphans.

### Accidental — found and fixed

| Accidental drift | Root cause | Fix |
|------------------|------------|-----|
| **`core-config.yaml` unparseable** | A parent-less stale list of protected paths (`- .aiox-core/infrastructure/**` …) plus an `exceptions:` key was left dangling under `autoClaude.qa`, a remnant of a relocated `boundary` block. js-yaml failed with *"bad indentation of a mapping entry (373:5)"*, so `loadConfig` silently fell back to built-in defaults (which carry a non-empty redirects map), corrupting every drift signal. | Removed the dead lines; the active `boundary:` config (top-level, line ~168) is unaffected. The drift report above is the result **after** this fix. |

No agent **content** required re-sync — once the config parsed correctly, drift was already 0 across all generated targets.

---

## 5. failOnDrift Smoke Test (AC5)

Verify-only proof that the `failOnDrift` / `--strict` mechanism actually fires:

```
# Baseline (no drift)
$ node .aiox-core/infrastructure/scripts/ide-sync/index.js validate --ide codex --strict
  → exit code 0

# Inject drift into a synced target file
$ printf '\n<!-- SIMULATED DRIFT -->\n' >> .codex/agents/dev.md
$ node .aiox-core/infrastructure/scripts/ide-sync/index.js validate --ide codex --strict
  → "| Drift | 1 |"  +  "Validation failed in strict mode"  +  exit code 1

# Restore
$ git checkout .codex/agents/dev.md   # (or restore from backup)
$ node ... validate --ide codex --strict
  → exit code 0
```

**Result:** drift present → **exit code 1** (gate fires); drift absent → **exit code 0**. The mechanism is proven live.

---

## 6. No-Regression (AC6)

All 10 activable agents (`@dev`, `@qa`, `@pm`, `@po`, `@sm`, `@architect`, `@analyst`, `@data-engineer`, `@ux-design-expert`, `@devops`) are present in the source **and** in both claude-code targets (command + skill), with 0 drift. No target lost legitimate files; only the dead YAML in the config and the redirect-map comment were touched.

---

## 7. How to Run the Drift Audit

```bash
# Full validation report (all targets)
node .aiox-core/infrastructure/scripts/ide-sync/index.js validate --verbose

# CI / gate mode (exit 1 on drift)
node .aiox-core/infrastructure/scripts/ide-sync/index.js validate --strict

# Single target
node .aiox-core/infrastructure/scripts/ide-sync/index.js validate --ide codex --strict

# Re-sync from source (fixes accidental drift the canonical way — never edit targets by hand)
node .aiox-core/infrastructure/scripts/ide-sync/index.js sync

# Automated test backstop (this story's AC1/AC2/AC5/AC6)
node --test tests/agents/agent-drift-audit.test.js
```

> The pipeline's runtime deps (`js-yaml`, `fs-extra`) resolve from `.aiox-core/node_modules`.

---

## 8. Cross-references

- **Folder/layer map:** [`docs/ARCHITECTURE.md`](../ARCHITECTURE.md) §4 (ideSync targets)
- **ideSync config:** `core-config.yaml → ideSync`
- **Pipeline:** `.aiox-core/infrastructure/scripts/ide-sync/`
- **Constitution:** `.aiox-core/constitution.md` (Art. II — Agent Authority; Art. VI–VII — Framework Boundary)
- **Test:** `tests/agents/agent-drift-audit.test.js`

---

## 9. Story 13.1 Audit — Shim Consolidation (2026-06-21)

> **Story:** [13.1 — Agent Definition Shim Consolidation](../stories/13.1.agent-shim-consolidation.story.md) · **Epic:** EPIC-13 · **Auditor:** @dev (Dex)

Story 13.1 re-audited the four agent-definition locations. Findings below
**confirm and extend** §1–§4; they do not contradict them.

### 9.1 Source of truth (AC1) — confirmed

The canonical source is **`.aiox-core/development/agents/` (12 `*.md`)**, declared
in `core-config.yaml → ideSync.source`. SKILLs are **generated** from this source
by the ide-sync pipeline — they are **not** manually maintained and **not** an
independent master. This matches §1. No generator named `generate-skills.js`
exists; generation is the `ide-sync` pipeline
(`.aiox-core/infrastructure/scripts/ide-sync/index.js`, `commandSync`).

### 9.2 Three agent systems, not one (AC3 correction)

The story premise ("`.claude/agents/*.md` are dead copies of L2") is **incorrect**.
There are **three distinct agent systems**, only one of which is sourced from L2:

| Location | System | Sourced from L2? | Action |
|----------|--------|------------------|--------|
| `.claude/commands/AIOX/agents/{id}.md` | ideSync command shim | ✅ Yes (target) | Drift-audited (§3) |
| `.claude/skills/AIOX/agents/{id}/SKILL.md` | ideSync runtime skill | ✅ Yes (target) | Drift-audited (§3) |
| `.claude/agents/{name}.md` | **Claude Code Task subagents** | ❌ **No** — independent | **Keep — do not deprecate** |

`.claude/agents/*.md` (e.g. `aiox-dev.md`, `aiox-qa.md`, `squad.md`) are **native
Claude Code subagent definitions** — YAML frontmatter with `tools`,
`permissionMode`, `hooks`, `skills`, used by the Task tool to spawn autonomous
agents. They use a different naming (`aiox-dev` vs L2 `dev`) and a different
purpose. **They are active, not dead.** Deprecating or deleting them (as AC3's
literal wording suggests) would break the autonomous-agent spawn system. The
correct action is this classification note — not a deprecation. They are
**out of scope** for ideSync drift enforcement.

### 9.3 Live drift detected (AC2)

The drift audit (`node --test tests/agents/agent-drift-audit.test.js`) currently
**FAILS** with **1 accidental content drift**:

| Target | File | Nature |
|--------|------|--------|
| `claude-code-skills` | `.claude/skills/AIOX/agents/sm/SKILL.md` | Generated SKILL diverges from L2 `sm.md` |

Direction of drift: the **generated SKILL is *ahead* of the source** — it carries
hand-added explicit dependency paths (`.aiox-core/product/templates/story-tmpl.yaml`,
explicit task-path comments) that the L2 `sm.md` source does not yet produce.
i.e. the target was hand-edited to be *more correct* and the source was never
updated to match.

**Remediation is BLOCKED for @dev:** the canonical fix
(`ide-sync sync --ide claude-code`) regenerates the SKILL from the staler source
and would **delete** the better hand-edits. The correct fix — porting the
explicit-path improvement back into `.aiox-core/development/agents/sm.md` (L2) —
requires a write to an L2 path denied to @dev by `settings.json` and the
Art. VI–VII boundary gate. **Routed upstream** via
`@aiox-master *propose-modification` (see §9.5).

### 9.4 Pre-commit gate (AC4) — designed, install BLOCKED

A dependency-free gate `validate-agent-sync.cjs` was designed to enforce, on
`git commit`: (1) every L2 source agent has both a command shim and a SKILL,
(2) deep content-drift detection delegated to the existing ide-sync validator
(graceful skip if `js-yaml` unavailable). Its target directory **`.claude/hooks/`
is in the `settings.json` deny list** (and protected by the boundary gate), so
@dev cannot install it. **Routed upstream** (see §9.5). Until installed, the
existing test `tests/agents/agent-drift-audit.test.js` is the drift backstop —
run it in CI.

### 9.5 Boundary blockers — upstream routing required

Story 13.1's literal ACs target paths @dev is constitutionally forbidden to write:

| AC | Literal target | Layer | Status | Resolution path |
|----|----------------|-------|--------|-----------------|
| AC4 | `.claude/hooks/validate-agent-sync.cjs` | protected (deny) | BLOCKED | `@aiox-master *propose-modification` |
| AC5 | `.aiox-core/core/README.md` | **L1** (deny) | BLOCKED + wrong location | document in **this** L4 doc instead |
| AC2 fix | `.aiox-core/development/agents/sm.md` | **L2** (deny) | BLOCKED | `@aiox-master *propose-modification` |

AC5's literal target (`.aiox-core/core/README.md`) is both boundary-blocked and
the **wrong location** — that README documents the core *runtime module*
(Story 2.2), unrelated to agent source-of-truth. The agent source-of-truth is
**already** canonically documented in **this** document (`AGENT-SOURCE-OF-TRUTH.md`,
L4). AC5 is therefore satisfied here, not in `core/README.md`.
