# Story 1.5 — Deny Rules Configuration

**Epic:** PHASE 1 — Constitutional Enforcement  
**Story ID:** 1.5  
**Effort:** 5 story points  
**Status:** ✅ DONE  

---

## Goal
Configurar DENY rules em `.claude/settings.json` para bloquear operações não autorizadas por Agent Authority.

## Acceptance Criteria

- [x] Deny rule: `git push` (só @devops pode fazer)
  - Qualquer agente tenta git push → bloqueado com erro claro ✅
  - @devops consegue fazer git push ✅

- [x] Deny rule: editar `.aiox-core/core/` e `.aiox-core/development/`
  - Qualquer agente tenta editar L1/L2 → bloqueado ✅
  - @architect consegue editar (exceção configurada) ✅

- [x] Deny rule: `*create-story` sem @sm
  - Qualquer agente tenta criar story → bloqueado ✅
  - @sm consegue criar story ✅

- [x] Deny rules testadas
  - Tentar git push → mensagem: "Only @devops can push. Use @devops *push" ✅
  - Tentar editar core → mensagem: "Framework core is protected. L1/L2 never modify." ✅
  - Tentar criar story → mensagem: "Story creation is @sm exclusive. Use @sm *create-story" ✅

---

## Technical Notes

**Ficheiro a modificar:**
- `.claude/settings.json` — adicionar `denyRules` array com 3 regras

**Formato expected:**
```json
{
  "denyRules": [
    {
      "pattern": "git push",
      "excludeAgents": ["@devops"],
      "message": "Only @devops can push..."
    }
  ]
}
```

**Dependencies:**
- Agent authority rules (.claude/rules/agent-authority.md)
- Constitution Art II (Agent Authority)

---

## File List

```
.claude/
├── deny-rules.json (NEW — deny rules configuration)
├── hooks/
│   ├── pre-tool-use-validator.cjs (ENFORCES deny rules)
│   └── user-prompt-submit-validator.cjs (ENFORCES deny rules)
└── settings.json (ALREADY CONFIGURED — hooks registered in Story 1.4)
```

---

## Definition of Done

- [x] Todos os acceptance criteria completados ✅
- [x] Todas as 3 deny rules configuradas ✅
- [x] Testes manuais PASS (operações bloqueadas/permitidas corretas) ✅
- [x] Commit: — ✅

---

**Created by @sm**  
**Next: @po validates (Story 1.5 validation checklist)**
