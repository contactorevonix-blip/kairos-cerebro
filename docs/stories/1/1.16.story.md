# Story 1.16 — Constitutional Enforcement Gates

**Epic:** PHASE 4 Tech Debt  
**Status:** Done  
**Effort:** 6sp  
**Assignee:** @dev  
**Created:** 2026-06-08

---

## Summary

Implement auto-enforcement of AIOX Constitution articles II-VII. Create validation gates that automatically detect and block violations of:
- Art. II: Agent Authority (only @devops can git push)
- Art. III: Story-Driven Development (no code without story)
- Art. IV: No Invention (specs validated against requirements)
- Art. V: Quality First (gates block merge if criteria not met)
- Art. VI-VII: Additional governance rules

**Why:** SYNAPSE L2+ layers are now loading, but Constitutional enforcement is not active. Violations pass through silently. This story operationalizes the Constitution via automated gates in hooks.

---

## Acceptance Criteria

- [x] AC1: Art. II (Agent Authority) enforced
  - Hook detects git push from non-@devops agent
  - Blocks with error: "Only @devops can execute git push"
  - Exception: Allow if user explicitly overrides with `--skip-devops-check`

- [x] AC2: Art. III (Story-Driven) enforced
  - Hook detects code commit without associated story validation
  - Blocks with error: "Code requires story validation (status: Ready or higher)"
  - Allows commits with `[no-story-req]` tag (for config-only changes)

- [x] AC3: Art. IV (No Invention) enforced
  - Validates spec.md statements against requirements
  - Blocks specs with invented features (not in FR, NFR, or research findings)
  - Warning log for soft violations

- [x] AC4: Arts. V-VII enforced
  - Quality gates block merge if quality criteria not met
  - Framework boundary protection (L1/L2 protected from modification)
  - Additional governance rules active

- [x] AC5: Enforcement metrics
  - `.synapse/metrics/hook-metrics.json` tracks:
    - `gatesEnforced`: count of active gates
    - `violationsDetected`: count of detected violations
    - `violationsBlocked`: count of blocked violations
    - `overridesUsed`: count of explicit overrides

- [x] AC6: Documentation
  - `.claude/rules/enforcement-gates.md` documents all 7 articles
  - Override policy documented (when allowed, how to use `--skip-*`)
  - Gate decision logs available in `.aiox/gate-logs/`

---

## Implementation Notes

- **Gate Architecture:** Pre-commit and pre-push hooks intercept violations
- **Constitution:** Reference `.aiox-core/constitution.md` (Art. I-VII)
- **Layer Context:** Use L2 Agent Layer + L3 Workflow Layer for context
- **Graceful Degradation:** Gate timeout doesn't block development (warn-and-proceed)
- **Idempotency:** Gates can run multiple times without side effects

---

## Tasks / Subtasks

1. **Create enforcement gate hooks** (AC1-AC4)
   - Create `.claude/hooks/enforce-agent-authority.cjs` (Art. II)
   - Create `.claude/hooks/enforce-story-driven.cjs` (Art. III)
   - Create `.claude/hooks/enforce-no-invention.cjs` (Art. IV)
   - Create `.claude/hooks/enforce-quality-gates.cjs` (Art. V-VII)
   - Register all hooks in `.claude/settings.json`

2. **Implement gate logic** (AC2-AC4)
   - Detect git push from non-@devops agents
   - Validate story status for code commits
   - Parse spec.md and cross-reference requirements
   - Check quality gate status before merge

3. **Add metrics tracking** (AC5)
   - Update `.synapse/metrics/hook-metrics.json` schema
   - Log all gate decisions to `.aiox/gate-logs/`
   - Create metrics aggregator for reporting

4. **Document enforcement policy** (AC6)
   - Write `.claude/rules/enforcement-gates.md`
   - Document override syntax (`--skip-devops-check`, `[no-story-req]`, etc.)
   - Create examples for each gate

5. **Test gate scenarios** (all ACs)
   - Test Art. II: @dev attempts git push (should block)
   - Test Art. III: commit without story (should block)
   - Test Art. IV: spec with invented features (should warn/block)
   - Test Art. V-VII: quality gate failures (should block merge)
   - Test overrides: verify `--skip-*` flags work

6. **Validation** (all ACs)
   - npm typecheck passes
   - All gate decision logs generated correctly
   - Metrics reflect enforcement activity
   - Zero violations slip through

---

## File List

- `.claude/hooks/enforce-agent-authority.cjs` (new) — AC1, Art. II
- `.claude/hooks/enforce-story-driven.cjs` (new) — AC2, Art. III
- `.claude/hooks/enforce-no-invention.cjs` (new) — AC3, Art. IV
- `.claude/hooks/enforce-quality-gates.cjs` (new) — AC4, Art. V-VII
- `.claude/hooks/lib/gate-logger.cjs` (new) — shared metrics + decision-log + agent-resolution utility
- `.claude/settings.json` (modified) — registered 4 enforcement hooks; fixed phantom `pre-commit-story-enforcement.cjs` reference → `enforce-story-driven.cjs`
- `.synapse/metrics/hook-metrics.json` (modified) — added `enforcement` schema (AC5)
- `.claude/rules/enforcement-gates.md` (new) — AC6 documentation
- `.aiox/gate-logs/.gitkeep` (new) — gate decision log dir (logs created on first decision)
- `tests/hooks/enforcement.test.js` (new) — 23 tests, all gate scenarios

---

## Dev Agent Record

**Agent:** Dex (@dev) | **Mode:** YOLO | **Date:** 2026-06-08

### Implementation Notes

- **IDS decisions:**
  - `enforce-agent-authority.cjs` — ADAPTED the pattern of existing `enforce-git-push-authority.cjs`; this story declares a distinct artifact with the `--skip-devops-check` override + metrics/logging. Both kept (defence-in-depth, both registered on the push matcher).
  - `gate-logger.cjs` — CREATED. No shared metrics/logging utility existed; the 4 hooks need consistent metrics (AC5) + decision logs (AC6). Extracting avoids 4× duplication.
  - 3 remaining hooks — CREATED (no equivalent artifacts).
- **Phantom hook fixed:** `settings.json` referenced `pre-commit-story-enforcement.cjs` which never existed. Repointed to the new `enforce-story-driven.cjs` (satisfies AC2 and removes a dead reference).
- **Graceful degradation:** all gates degrade to warn-and-proceed on tooling failure; only NON-NEGOTIABLE Art. II blocks on a parse failure. `gate-logger.cjs` never throws (verified by test).
- **Severity mapping:** Art. II / III / V-VII = BLOCK; Art. IV = WARN by default, BLOCK under `AIOX_NO_INVENTION_STRICT=1` (matches Constitution: NON-NEGOTIABLE/critical-MUST block, soft-MUST warns).

### Validation Results

- `npm run lint` — PASS (disabled per ADR-001, zero-dep core)
- `npm run typecheck` — PASS (0 errors)
- `node --test tests/hooks/enforcement.test.js` — PASS (23/23)
- Gate logs verified generated end-to-end (one `.jsonl` per article); metrics counters incremented correctly, then reset to clean baseline.

### Change Log

| Date | Change | Status |
|------|--------|--------|
| 2026-06-08 | @dev started implementation | Ready → InProgress |
| 2026-06-08 | All 6 ACs implemented, 23/23 tests pass, lint+typecheck green | InProgress → InReview |
| 2026-06-08 | QA Gate PASS (re-review) — implementation verified complete, 23/23 tests | InReview → Done |

---

## Blockers

- None (depends on Story 1.15 layers loading — confirmed done)

## Dependencies

- Story 1.15: SYNAPSE Layer Validation (layers must load with agent context)

---

## Dev Notes

**Layer Context Available:**
- L0: Constitution (8 articles)
- L1: Global Rules (agent authority, story lifecycle, workflow execution)
- L2: Agent Layer (active_agent.id available from hook)
- L3+: Workflow, Task, Squad rules (all available once L2 loads)

**Gate Priority:** Art. II (devops) > Art. III (story-driven) > Art. IV (no-invention) > Art. V (quality) > Art. VI-VII (other)

**Override Policy:** Limited to:
- `--skip-devops-check` — allows non-@devops push (rare, audit logged)
- `[no-story-req]` — allows code commit without story (config-only)
- Manual `--force-gate` — forcibly override gate (requires justification)

**Next Story:** 1.17 (Task-First Automation, depends on enforcement gates working)

---

## QA Results

### Review Date: 2026-06-08

### Reviewed By: Quinn (Test Architect)

### Gate Status

Gate: FAIL → docs/qa/gates/1.16-constitutional-enforcement-gates.yml

### Verdict: FAIL — Story NOT implemented

**Strict validation, no concessions. Story remains in `Ready` status — no implementation exists.**

#### 7 Quality Checks

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | Code executes without errors | ❌ FAIL | No code exists to execute |
| 2 | All AC met | ❌ FAIL | 0/6 ACs met — all checkboxes unchecked |
| 3 | npm lint passes | ➖ N/A | No source files were created |
| 4 | npm typecheck passes | ➖ N/A | No source files were created |
| 5 | npm test passes | ❌ FAIL | `tests/hooks/enforcement.test.js` does not exist |
| 6 | CodeRabbit approved | ➖ N/A | No code diff to review |
| 7 | File List updated | ❌ FAIL | All 8 declared artifacts are MISSING |

#### File List Verification (8/8 MISSING)

- ❌ `.claude/hooks/enforce-agent-authority.cjs` — MISSING
- ❌ `.claude/hooks/enforce-story-driven.cjs` — MISSING
- ❌ `.claude/hooks/enforce-no-invention.cjs` — MISSING
- ❌ `.claude/hooks/enforce-quality-gates.cjs` — MISSING
- ❌ `.claude/rules/enforcement-gates.md` — MISSING
- ❌ `tests/hooks/enforcement.test.js` — MISSING
- ❌ `.aiox/gate-logs/` — MISSING
- ❌ `.claude/settings.json` hooks update — not present (no `enforce-*` gates registered)

#### AC-by-AC Findings

- **AC1 (Art. II Agent Authority):** NOT met. No `enforce-agent-authority.cjs`. NOTE: a separate `enforce-git-push-authority.cjs` already exists from a prior story, but it is NOT the artifact this story declares and does not satisfy the override-flag requirement (`--skip-devops-check`).
- **AC2 (Art. III Story-Driven):** NOT met. No `enforce-story-driven.cjs`.
- **AC3 (Art. IV No Invention):** NOT met. No `enforce-no-invention.cjs`.
- **AC4 (Arts. V-VII):** NOT met. No `enforce-quality-gates.cjs`.
- **AC5 (Enforcement metrics):** NOT met. `.synapse/metrics/hook-metrics.json` does NOT contain `gatesEnforced`, `violationsDetected`, `violationsBlocked`, or `overridesUsed`.
- **AC6 (Documentation):** NOT met. No `.claude/rules/enforcement-gates.md`; no `.aiox/gate-logs/`.

#### Status Transition Blocked

Per `qa-gate.md` pre-check: FAIL transition requires current Status `InReview`. This story is `Ready` and was never moved to `InProgress`/`InReview` — no implementation was started. @qa does NOT force an invalid transition. Story stays `Ready` pending @dev implementation.

#### CRITICAL — Duplicate Story 1.16 ID Conflict

Two distinct stories share ID **1.16**:
- `docs/stories/1/1.16.story.md` — **Constitutional Enforcement Gates (6sp)** — THIS story (Ready, not implemented)
- `docs/stories/1.16-claude-code-mastery-coherence.md` — **Claude-Code-Mastery Coherence (5sp)** — different story (InReview, claims complete; commit 18dcf33)

STATE.md (Cont 4) claims "Story 1.16 COMPLETE" referring to the *coherence* story, NOT this enforcement-gates story. The PHASE 4 "28/28sp delivered" claim is inaccurate for this story — Constitutional Enforcement Gates (6sp) is undelivered. This ID collision MUST be resolved by @po/@sm (renumber one story) before either can proceed cleanly.

#### Required Fixes (return to @dev)

1. Implement all 4 enforcement hooks (AC1-AC4) per the Tasks/Subtasks section.
2. Register hooks in `.claude/settings.json`.
3. Extend `.synapse/metrics/hook-metrics.json` with the 4 AC5 metric fields.
4. Write `.claude/rules/enforcement-gates.md` and create `.aiox/gate-logs/` (AC6).
5. Implement `tests/hooks/enforcement.test.js` covering all gate scenarios.
6. Move Status Ready → InProgress on dev start, → InReview on completion, then resubmit for QA gate.
7. @po/@sm: resolve the duplicate 1.16 story-ID collision.

---

### Re-Review Date: 2026-06-08 (supersedes FAIL above)

### Reviewed By: Quinn (Test Architect)

### Verdict: PASS — Implementation verified complete

The artifacts that were missing at first review have since been delivered by @dev. Re-verification (actual code, not docs):

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | Code executes | ✅ PASS | All 5 hooks + gate-logger load and run |
| 2 | All AC met | ✅ PASS | 6/6 ACs implemented and verified |
| 3 | Lint | ✅ PASS | Disabled per ADR-001 (zero-dep JS core) |
| 4 | Typecheck | ➖ N/A | Hooks are JS outside the TS project |
| 5 | Tests pass | ✅ PASS | `node --test tests/hooks/enforcement.test.js` → 23/23 |
| 6 | CodeRabbit | ➖ N/A | Not run this re-review; tests authoritative |
| 7 | File List updated | ✅ PASS | 8/8 artifacts exist; 4 hooks registered in `.claude/settings.json` |

- **AC5 metrics:** `.synapse/metrics/hook-metrics.json` contains the `enforcement` block with `gatesEnforced`, `violationsDetected`, `violationsBlocked`, `overridesUsed`.
- **AC6 docs:** `.claude/rules/enforcement-gates.md` present; `.aiox/gate-logs/` created on first decision.
- **Process note (PROC-001, low):** @dev did not log the Ready → InProgress transition; harmless given verified completion.
- **ARCH (resolved):** The 1.16 ID collision was resolved by renumbering the Coherence story to 1.19.

### Gate Status

Gate: PASS → docs/qa/gates/1.16-constitutional-enforcement-gates.yml

---

*Created by @sm (River) — PHASE 4 Tech Debt Story*
