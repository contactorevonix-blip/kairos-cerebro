# Story 7.7: `.claude/settings.local.json` — Restaurar `permissions.allow`

**Status:** Draft  
**Story ID:** 7.7  
**Created:** 2026-06-10  
**Epic:** Epic-7 — AIOX Core Realignment  
**ADE Owner:** @hooks-architect  
**Quality Gate:** @qa  
**Story Points:** 2

---

## Summary

Restaurar `permissions.allow` no ficheiro `.claude/settings.local.json` (gitignored, seguro para modificar). Local tem configuração invertida: só contém `hooks` (6 entries mal-colocadas); deveria ter `permissions.allow` (8 entradas upstream: npm lint/test, git add/commit/push, node -e). Coordenar com Story 6.1.

🔴 **CRÍTICO:** Utilizador terá de aprovar repetidamente para operações git/npm quando deveria estar tudo white-listed.

---

## Problem Statement

**Current State:**
- `.claude/settings.local.json` (gitignored) — configuração **invertida**
- **Upstream seed:** contém `permissions.allow` com 8 entradas permitidas (npm lint, npm test, git add, git commit, git push, node -e)
- **Local actual:** contém **só `hooks`** com 6 entries em `UserPromptSubmit` (duplicam lógica de Story 6.1)
- **Resultado:** Utilizador pede aprovação manualmente para cada comando git/npm

**Upstream Seed Content:**
```json
{
  "permissions": {
    "allow": [
      "npm lint",
      "npm test",
      "git add",
      "git commit",
      "git push",
      "node -e"
    ]
  }
}
```

**Root Cause:** Story 6.1 (ADE hooks) concentrou em hooks; permissions foram removidas mas nunca restauradas.

**Source:** AIOX-CORE-REALIGNMENT-AUDIT-2026-06-10.md, Gap B.3 (CRÍTICA)

---

## Acceptance Criteria

- [ ] **AC1:** Ler upstream seed de `.claude/settings.local.json` (via `gh api` ou documentação)
- [ ] **AC2:** Extrair 8 entradas `permissions.allow`
- [ ] **AC3:** Adicionar a `permissions.allow` em `.claude/settings.local.json` local
- [ ] **AC4:** Revisar se `hooks` em `settings.local.json` duplicam lógica de Story 6.1 (e remover se sim)
- [ ] **AC5:** Testar que comandos git/npm deixam de pedir aprovação manual (dependendo de permissão mode)
- [ ] **AC6:** Confirmar que `.claude/settings.local.json` continua gitignored (seguro)

---

## Scope

### **IN**
1. `.claude/settings.local.json` (gitignored) — restaurar `permissions.allow`
2. Coordenação com Story 6.1 (ADE hooks, mesma dependência)

### **OUT**
- Alterações em `.claude/settings.json` (ficheiro global, não local)
- Alterações em `.husky/` ou hooks propriamente ditos

---

## Technical Context

**File Reference:** `.claude/settings.local.json` (gitignored, safe to edit)

**Upstream Seed:**
```json
{
  "permissions": {
    "allow": [
      "npm run lint",
      "npm run test",
      "git add",
      "git commit",
      "git push",
      "node -e"
    ]
  }
}
```

**Note:** Coordenar com `@hooks-architect` (owner de Story 6.1) para determinar se `hooks` bloqueiam ou complementam `permissions.allow`.

---

## Tasks / Subtasks

1. **Fetch upstream seed** — Verificar configuração esperada de `.claude/settings.local.json` upstream
2. **Extract permissions** — Listar os 8 comandos permitidos
3. **Read local config** — Verificar `hooks` actuais em `.claude/settings.local.json`
4. **Restore permissions.allow** — Adicionar secção `permissions.allow` com 8 entries
5. **Review hooks** — Determinar se `hooks` duplicam lógica de Story 6.1; remover se sim
6. **Test** — Confirmar que permissões funcionam

---

## File List

| File | Action | Status |
|------|--------|--------|
| `.claude/settings.local.json` | Edit (restore permissions) | [ ] |

---

## Dependencies

**Coordenar com:** Story 6.1 — `@hooks-architect` é ADE owner de ambas as stories (6.1 + 7.7). Decision: 7.7 é story standalone ou AC adicional de 6.1? A ser decidido por `@po` na validação.

---

## Change Log

- 2026-06-10: Story created by @sm (audit-driven, from AIOX-CORE-REALIGNMENT-AUDIT Gap B.3, CRÍTICO, coordena com Story 6.1)
