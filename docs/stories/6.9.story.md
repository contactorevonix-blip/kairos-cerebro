# Story 6.9: Shell Hook Wrappers Cleanup

**Status:** Ready  
**Story ID:** 6.9  
**Created:** 2026-06-10  
**Epic:** Epic-6 — AIOX Synchronization & Integration Audit Remediation  
**ADE Owner:** @hooks-architect  
**Quality Gate:** @qa  
**Story Points:** 1

---

## Summary

Review 2 orphaned shell script hook files in `.claude/hooks/` not referenced in settings:
- `enforce-git-push-authority.sh`
- `pre-commit-version-check.sh`

The Node.js equivalents (`.cjs` files) are active. Determine if shell wrappers should be: (A) activated for native git hooks (`.git/hooks/`), (B) archived, or (C) deleted.

**Impact:** LOW — Cleanup only (zero functional impact).

---

## Problem Statement

**Current State:**
- 2 shell scripts exist in `.claude/hooks/`
- Not referenced in any settings.json hook configuration
- Node.js equivalents (`.cjs` files) are active and working
- Likely superseded by Node implementations OR meant for `.git/hooks/` wrapper layer

**What's Missing:**
- Decision on whether these are needed or can be removed

**Source:** AIOX-SYNC-AUDIT-2026-06-10.md, Gap 3.2 (lines 245–263)

---

## Acceptance Criteria

- [ ] **AC1:** Confirm 2 shell scripts exist and are not in settings config
- [ ] **AC2:** Verify corresponding Node.js .cjs files are active and working
- [ ] **AC3:** Determine purpose: (A) native git hooks, (B) legacy, or (C) unused?
- [ ] **AC4:** Get decision from Pedro: Keep (activate), Archive, or Delete?
- [ ] **AC5:** If keeping: Integrate into `.git/hooks/` or settings appropriately
- [ ] **AC6:** If removing: Archive to `.claude/hooks/_archive/` or delete

---

## Scope

### **IN**
1. `.claude/hooks/enforce-git-push-authority.sh`
2. `.claude/hooks/pre-commit-version-check.sh`

### **OUT**
- Node.js equivalents (`.cjs` files are active)
- `.git/hooks/` directory (only if integrating)

---

## Technical Context

**Files:**
- `enforce-git-push-authority.sh` — wrapper for `enforce-git-push-authority.cjs` (active)
- `pre-commit-version-check.sh` — no obvious equivalent, likely unused

**Decision Options:**
1. **Keep for native git hooks:** Integrate into `.git/hooks/pre-commit` or `.git/hooks/pre-push`
2. **Archive:** Move to `.claude/hooks/_archive/` for reference
3. **Delete:** Remove if confirmed no consumers

---

## Tasks / Subtasks

1. **List 2 shell files** — Confirm and check for references
2. **Identify equivalents** — Map to corresponding .cjs files
3. **Understand purpose** — Read comments/code in each
4. **Get user decision** — Ask Pedro (keep for .git/hooks, archive, or delete)
5. **Execute decision**:
   - If keep: Integrate into `.git/hooks/` or settings
   - If archive: Move to _archive/
   - If delete: Remove
6. **Update File List** (at end of story)

---

## File List

| File | Action | Status | Decision |
|------|--------|--------|----------|
| `.claude/hooks/enforce-git-push-authority.sh` | Archive or Delete | [ ] | Pending |
| `.claude/hooks/pre-commit-version-check.sh` | Archive or Delete | [ ] | Pending |

---

## Change Log

- 2026-06-10: Story created by @sm
