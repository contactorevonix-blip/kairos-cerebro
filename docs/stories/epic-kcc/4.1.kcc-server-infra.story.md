# Story 4.1: KCC — Server Infrastructure + Write Guard + WORKFLOW-STATE

## Status
**Done**

## Executor Assignment
```
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["node bin/kairos-command-center.js", "curl localhost:3001/api/health", "curl -X PUT localhost:3001/api/write/content -d '{\"path\":\".aiox-core/x\"}' → expect 403"]
```

## Story

**As a** Pedro (solo founder),
**I want** poder iniciar o KCC com `node bin/kairos-command-center.js` e ver o app a correr em localhost:3001,
**so that** tenha a infraestrutura base que todas as outras stories do Sprint 1 dependem.

## Epic Context

- **Epic:** EPIC-004 — KAIROS COMMAND CENTER
- **Sprint:** 1 (Infraestrutura base — deve ser implementada PRIMEIRO)
- **PRD Reference:** EPIC-KCC.md, FR-1, NFR-1, NFR-2
- **Complexidade:** M (5 pts)
- **Depends on:** — (primeira story do epic, sem dependências)
- **Blockers para:** 4.2, 4.3, 4.4 (dependem desta infra)

## Acceptance Criteria

1. `node bin/kairos-command-center.js` inicia servidor Express na porta 3001 sem erros
2. Browser abre automaticamente em `http://localhost:3001` (via `exec('start http://...')`)
3. `http://localhost:3001` serve `packages/kcc/public/index.html` com 9 tabs visíveis
4. CSS design tokens carregados — background da página é `#080A10` (verificável no DevTools)
5. `GET /api/health` responde `200` com envelope `{ ok: true, data: { pass: 15, warn: 0, fail: 0 } }`
6. `GET /api/state` responde `200` com conteúdo de `.aiox/WORKFLOW-STATE.json`
7. `.aiox/WORKFLOW-STATE.json` criado automaticamente se não existir, com schema completo
8. `PUT /api/write/content` com `path: ".aiox-core/constitution.md"` responde `403 WRITE_GUARD_VIOLATION`
9. `PUT /api/write/content` com `path: "docs/stories/test.md"` responde `200 { ok: true }`
10. `.aiox/kcc-write-log.json` criado após qualquer tentativa de escrita (sucesso ou falha)

## Integration Verification

**IV1:** `node bin/kairos-command-center.js` — sem erros no stdout, porta 3001 activa
**IV2:** `curl localhost:3001/api/health` → `{"ok":true,"data":{"pass":15}}`
**IV3:** `curl -X PUT localhost:3001/api/write/content -H "Content-Type: application/json" -d '{"path":".aiox-core/constitution.md","content":"x"}' | jq .error.code` → `"WRITE_GUARD_VIOLATION"`
**IV4:** `curl -X PUT localhost:3001/api/write/content -H "Content-Type: application/json" -d '{"path":"docs/stories/kcc-test.md","content":"# test"}' | jq .ok` → `true`
**IV5:** `.aiox/WORKFLOW-STATE.json` existe com campos: `activeStory`, `phase`, `activeAgent`, `nextExpectedAction`, `pendingGates`, `lastUpdated`

## Tasks / Subtasks

- [x] Criar `packages/kcc/package.json` (name: @kairos/kcc, deps: express, js-yaml, chokidar)
- [x] `npm install` em `packages/kcc/`
- [x] Criar `packages/kcc/server.js` — Express app com middleware + rotas stub
- [x] Criar `bin/kairos-command-center.js` — entry point que inicia server + abre browser
- [x] Criar `packages/kcc/lib/write-guard.js` — allowlist L4 + blocklist + audit log
- [x] Criar `packages/kcc/lib/workflow-state.js` — lê/escreve WORKFLOW-STATE.json
- [x] Criar `packages/kcc/routes/health.js` — GET /api/health (aiox doctor light)
- [x] Criar `packages/kcc/routes/state.js` — GET /api/state
- [x] Criar `packages/kcc/routes/write.js` — PUT /api/write/content (com guard)
- [x] Criar stubs para routes restantes: files.js, agents.js, stories.js, metrics.js, discuss.js
- [x] Criar `packages/kcc/public/index.html` — app shell com 9 tabs + layout 3 colunas
- [x] Criar `packages/kcc/public/style.css` — design tokens de Uma + layout base
- [x] Criar `packages/kcc/public/app.js` — tab switching + fetch wrapper
- [x] Criar `.aiox/WORKFLOW-STATE.json` com valores iniciais reais
- [x] Verificar IV1 a IV5 — todos PASS

## Dev Notes

**Entry point** (`bin/kairos-command-center.js`):
```javascript
const path = require('path');
const { createServer } = require('./packages/kcc/server');
const PORT = process.env.KCC_PORT || 3001;
const server = createServer(path.resolve(__dirname));
server.listen(PORT, () => {
  console.log(`KCC: http://localhost:${PORT}`);
  require('child_process').exec(`start http://localhost:${PORT}`);
});
```

**WORKFLOW-STATE.json** schema inicial:
```json
{
  "activeStory": "3.5",
  "phase": "Done",
  "activeAgent": null,
  "nextExpectedAction": "*qa-gate 3.5",
  "track": null,
  "pendingGates": ["qa-gate", "pre-push-quality-gate"],
  "lastUpdated": "<ISO timestamp>"
}
```

**Write guard paths** (ADR-4):
- Writable: `docs/`, `squads/`, `packages/`, `bin/kairos`
- Blocked: `.aiox-core/`, `.claude/settings.json`, `core-config.yaml`, `bin/aiox.js`

**Design tokens** (ADR-1 + Uma):
- CSS: `--kcc-bg-base: #080A10; --kcc-accent: #7C3AED;` (não hardcodar valores)
- Layout: topbar 48px, sidebar 160px, right panel 280px, status bar 28px

## File List

**Novos ficheiros a criar:**
- `bin/kairos-command-center.js`
- `packages/kcc/package.json`
- `packages/kcc/server.js`
- `packages/kcc/lib/write-guard.js`
- `packages/kcc/lib/workflow-state.js`
- `packages/kcc/routes/health.js`
- `packages/kcc/routes/state.js`
- `packages/kcc/routes/write.js`
- `packages/kcc/routes/files.js` (stub)
- `packages/kcc/routes/agents.js` (stub)
- `packages/kcc/routes/stories.js` (stub)
- `packages/kcc/routes/metrics.js` (stub)
- `packages/kcc/routes/discuss.js` (stub)
- `packages/kcc/public/index.html`
- `packages/kcc/public/style.css`
- `packages/kcc/public/app.js`
- `.aiox/WORKFLOW-STATE.json`
- `.aiox/kcc-write-log.json` (criado automaticamente no primeiro write)

## Change Log

| Data | Agente | Acção |
|------|--------|-------|
| 2026-06-03 | @sm (River) | Story criada — Sprint 1 Story 1/4 |
| 2026-06-03 | @dev (Dex) | Implementação completa — 17 ficheiros, 5/5 IVs PASS |
| 2026-06-03 | @qa (Quinn) | PASS WITH CONCERNS — 10/10 ACs, 2 MEDIUM + 2 LOW registados |

## QA Gate Report

**Veredicto:** PASS WITH CONCERNS
**Data:** 2026-06-03

**Concerns (tech debt Sprint 2):**
- MEDIUM: `lib/workflow-state.js:6` — DEFAULT_STATE hardcoded, ficará desactualizado
- MEDIUM: `routes/health.js:17` — denyCount regex impreciso
- LOW: `routes/state.js` — execSync sem try/catch individual por campo git
- LOW: Sem testes automatizados — criar em Sprint 2
