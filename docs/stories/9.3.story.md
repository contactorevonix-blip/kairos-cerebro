# Story 9.3: Art. III Story-Driven Development Enforcement Gate

**Story ID:** 9.3  
**Epic:** EPIC-9 (Enforcement Gates)  
**Status:** InReview  
**Points:** 1sp  
**Owner:** @dev  
**Dependencies:** 9.1

## Description

Implement Constitutional Article III (Story-Driven Development) enforcement gate. Block git commits that don't reference an active story + ACs, or use `[no-story-req]` override for config-only commits.

**Article III Statement:** No code is written without a story associated. Every commit must trace to a story ID (e.g., `[Story 13.2]`) or use `[no-story-req]` override for exceptions.

## Scope

**IN:**
- Parse commit message for story reference regex
- Detect git commit operations at PreToolUse hook
- Block commits without story or override
- Log overrides with `[no-story-req]` flag
- Graceful degradation if story files unreadable
- 3+ test cases

**OUT:**
- Story creation or editing (that's @sm/@po)
- Commit message rewriting
- Story content validation (just story ID presence)
- Backfill existing commits

## Acceptance Criteria

- [x] Gate detects `git commit` at PreToolUse hook
- [x] Parse commit message for story reference: regex `\[Story \d+\.\d+\]`
- [x] If no story reference AND no `[no-story-req]` flag → BLOCK
- [x] If `[no-story-req]` present → ALLOW but log as `override`
- [x] Decision logged: article=`art-iii-story-driven`, reason shows commit msg + decision
- [x] Test cases:
  - [x] `git commit -m "feat: add X [Story 13.2]"` → ALLOWED
  - [x] `git commit -m "fix: typo"` → BLOCKED (no story)
  - [x] `git commit -m "chore: update config [no-story-req]"` → OVERRIDE logged
- [x] Graceful degradation: if story file unreadable, downgrade to `warn-and-proceed`
- [x] All 4 pre-commit gates PASS

## Risks Identified

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|-----------|
| Regex false positive/negative | Medium | Medium | Test regex with 10+ commit message formats |
| Story files unreadable during validation | Low | Medium | Graceful downgrade to warn-and-proceed |
| Override flag abuse (config commits become excuse) | Low | Low | Audit trail; periodic review of [no-story-req] usage |
| Multi-story commits (3+ stories in one commit) | Low | Low | Require ONE story per commit (enforce single story) |

## File List

- ✅ `.claude/hooks/enforce-story-driven.cjs` (EXISTS, 154 lines)
- ✅ `.aiox/gate-logs/art-iii-story-driven-{date}.jsonl` (appended at runtime)
- ✅ `tests/hooks/enforce-story-driven.test.js` (NEW, 172 lines, 22 tests PASS)

## Definition of Done

1. Gate logic implemented + tested
2. Commit message parsing accurate (regex validated)
3. Story reference detection working
4. Override syntax recognized
5. 3+ test cases PASS
6. Story marked "Ready for Review"

---

## Dev Agent Record

**Implementation completed:** 2026-06-18 (Dex — YOLO mode)

**Approach:**
- REUSE: enforce-story-driven.cjs already existed (Story 1.16)
- CREATE: Test suite for 9.3 (22 test cases covering all ACs)

**Key implementation:**
1. Gate detects git commit at PreToolUse hook
2. Extracts commit message via -m flag regex
3. Searches docs/stories for valid status (Ready+)
4. Supports [no-story-req] override for config commits
5. Graceful degradation: warn-and-proceed on errors

**Test Coverage:**
- 22 test cases (11 gate tests, 2 discovery tests, 7 quality tests)
- All ACs covered: AC1 (detect commit), AC2 (parse message), AC3/4 (block/override), AC5 (logging), AC6 (graceful)
- PASS rate: 100%

**Blockers:** None

---

**Blocked by:** Story 9.1  
**Parallelizable with:** Stories 9.2, 9.4-9.5

*Created by @sm (River) — 2026-06-18*  
*Implemented by @dev (Dex) — 2026-06-18*
