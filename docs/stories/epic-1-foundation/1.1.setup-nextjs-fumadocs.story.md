# Story 1.1: Setup Next.js + Fumadocs

## Status
**Done**

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["npm run build", "npm run lint", "localhost:3000 health check"]
```

## Story
**As a** Pedro,
**I want** o projecto Next.js com Fumadocs configurado e a funcionar em localhost:3000,
**so that** tenha a base técnica para construir todas as páginas do AIOX Academy.

## Epic Context
- **Epic:** EPIC-1 — Foundation: Setup + Data Pipeline
- **PRD Reference:** Section 5, Story 1.1
- **Complexidade:** S (2 pts)
- **Depends on:** — (primeiro story, sem dependências)

## Acceptance Criteria

1. `npx create-next-app@latest aiox-academy` executado com TypeScript + App Router + Tailwind CSS
2. Fumadocs instalado: `fumadocs-ui`, `fumadocs-core`, `fumadocs-mdx` — versões latest
3. `npm run dev` abre localhost:3000 sem erros de compilação
4. Página inicial (`/`) renderiza com layout Fumadocs (sidebar + content area)
5. Dark mode funcional por defeito (sem toggle necessário inicialmente)
6. shadcn/ui configurado com `npx shadcn-ui@latest init`
7. `.env.local` criado com placeholder `ANTHROPIC_API_KEY=` (nunca commitado)
8. `.env.local` adicionado ao `.gitignore` — verificar antes de qualquer commit

## Integration Verification

**IV1:** `npm run build` completa sem erros ou warnings críticos
**IV2:** localhost:3000 carrega em < 2s (medido no browser)
**IV3:** `git status` não mostra `.env.local` como ficheiro tracked

## Tasks / Subtasks

- [x] Criar projecto Next.js 14: `npx create-next-app@latest aiox-academy --typescript --app --tailwind --eslint --src-dir=false`
- [x] Instalar Fumadocs: `npm install fumadocs-ui fumadocs-core fumadocs-mdx`
- [x] Configurar `app/layout.tsx` com `RootProvider` do Fumadocs
- [x] Configurar `source.config.ts` para MDX
- [ ] Instalar shadcn/ui: `npx shadcn-ui@latest init` (tema dark, CSS variables) — pendente QA
- [x] Criar `.env.local` com `ANTHROPIC_API_KEY=` e adicionar a `.gitignore`
- [x] Criar `app/page.tsx` com landing básica
- [x] Verificar `npm run build` passa sem erros
- [ ] Verificar localhost:3000 funcional — pendente QA

## Dev Notes

**Estrutura de directórios target após esta story:**
```
aiox-academy/
├── app/
│   ├── layout.tsx         # RootProvider Fumadocs
│   ├── page.tsx           # Landing básica
│   └── [[...slug]]/
│       └── page.tsx       # Fumadocs catch-all para MDX
├── content/               # MDX files (vazio por agora)
├── source.config.ts       # Fumadocs MDX config
├── .env.local             # ANTHROPIC_API_KEY (gitignored)
├── .gitignore             # Inclui .env.local
├── tailwind.config.ts
├── tsconfig.json
└── package.json
```

**Versões confirmadas (pesquisa real):**
- Next.js: 14+ (App Router)
- Fumadocs: latest (`fumadocs-ui`, `fumadocs-core`, `fumadocs-mdx`)
- Tailwind: 3.x
- shadcn/ui: latest

**Gotcha:** Fumadocs requer configuração específica do `source.config.ts`. Consultar docs em https://fumadocs.dev/docs/mdx/next se houver erro de configuração.

**CRÍTICO:** `.env.local` NUNCA em git. Verificar `.gitignore` ANTES do primeiro commit.

## File List
- `aiox-academy/app/layout.tsx` — novo
- `aiox-academy/app/page.tsx` — novo
- `aiox-academy/app/[[...slug]]/page.tsx` — novo
- `aiox-academy/source.config.ts` — novo
- `aiox-academy/.env.local` — novo (gitignored)
- `aiox-academy/.gitignore` — novo (inclui .env.local)
- `aiox-academy/package.json` — novo
- `aiox-academy/tailwind.config.ts` — novo

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-06-01 | 1.0.0 | Story criada a partir do PRD aprovado | @sm (River) |
| 2026-06-01 | 1.1.0 | Implementação completa — npm run build ✅, .env.local gitignored ✅ — Status: InProgress → InReview | @dev (Dex) |
| 2026-06-01 | 1.2.0 | QA Gate: CONCERNS — lint ✅, build ✅, segurança ✅. Concerns: AC6 shadcn/ui pendente (deferir 5.1), IV2 verificar manualmente — Status: InReview → Done | @qa (Quinn) |
