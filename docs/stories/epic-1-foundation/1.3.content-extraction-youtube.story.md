# Story 1.3: Content Extraction Pipeline — YouTube Transcripts

## Status
**Ready**

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["python scripts/extract-transcripts.py --dry-run", "output file count validation"]
```

## Story
**As a** Pedro,
**I want** um script que extrai as transcrições dos vídeos confirmados do Alan Nicolas (@oalanicolas),
**so that** tenha os insights práticos reais como base de conteúdo das lições, sem inventar o que ele ensina.

## Epic Context
- **Epic:** EPIC-1 — Foundation: Setup + Data Pipeline
- **PRD Reference:** Section 5, Story 1.3
- **Complexidade:** S (2 pts)
- **Depends on:** Story 1.1 (projecto existe)

## Acceptance Criteria

1. Script `scripts/extract-transcripts.py` executável com `python scripts/extract-transcripts.py`
2. Usa `youtube-transcript-api` (open source, sem API key, sem headless browser)
3. Extrai transcrições dos 6 vídeos confirmados:
   - `gEUtUdqiAyk` — "AIOX Squad: MAIS INTELIGENTE, ECONÔMICO e RÁPIDO"
   - `G2KcYWSyIbI` — "Crie seu próprio AIOX — Curso Gratuito Claude Code"
   - `Mm0pAaofPOo` — "Super Agentes do Alan Nicolas | Ep.01"
   - `cvba_dYjdEI` — "O Ano dos Agentes de IA"
   - `Ut4ecAzE7o8` — "AIOX: Criei um squad de IA para organizar dados"
4. Output: `content/raw/youtube/{video_id}.txt` (transcrição em texto plano)
5. Script imprime log: vídeos processados, eventuais falhas (sem legendas disponíveis)
6. Se transcrição indisponível para um vídeo: log warning, continuar os restantes

## Integration Verification

**IV1:** Script termina sem crash (exit code 0 mesmo se 1 vídeo falhar)
**IV2:** Pelo menos 4 de 6 ficheiros `.txt` criados em `content/raw/youtube/`
**IV3:** Cada ficheiro tem > 500 caracteres (conteúdo real, não vazio)

## Tasks / Subtasks

- [ ] Instalar dependência: `pip install youtube-transcript-api` (documentar em `requirements.txt`)
- [ ] Criar `scripts/extract-transcripts.py`
- [ ] Implementar loop pelos 6 video IDs
- [ ] Guardar transcrição como texto plano: `content/raw/youtube/{id}.txt`
- [ ] Try/except por vídeo: falha num não para os outros
- [ ] Log final: `{ success: N, failed: M, files: [...] }`
- [ ] Adicionar `requirements.txt` com `youtube-transcript-api==1.2.4`
- [ ] Testar com `--dry-run` flag (lista IDs mas não extrai)

## Dev Notes

**Ferramenta verificada (pesquisa real):**
- `youtube-transcript-api` v1.2.4 (Janeiro 2026)
- Python 3.8-3.14 compatível
- Sem API key, sem headless browser
- GitHub: https://github.com/jdepoix/youtube-transcript-api

**Uso básico:**
```python
from youtube_transcript_api import YouTubeTranscriptApi

def get_transcript(video_id):
    transcript = YouTubeTranscriptApi.get_transcript(video_id, languages=['pt', 'en'])
    return ' '.join([t['text'] for t in transcript])
```

**Video IDs confirmados (fontes verificadas com WebFetch):**
```python
VIDEO_IDS = {
    'gEUtUdqiAyk': 'AIOX Squad MAIS INTELIGENTE',
    'G2KcYWSyIbI': 'Crie seu proprio AIOX Curso Gratuito',
    'Mm0pAaofPOo': 'Super Agentes Ep01',
    'cvba_dYjdEI': 'O Ano dos Agentes de IA',
    'Ut4ecAzE7o8': 'AIOX squad dados saude',
}
```

**Nota:** Playlist Superagentes (`PLwMtuCbkLj_Is4knvSKClvCFjqG6LhHEn`) tem mais vídeos — adicionar IDs em iteração futura.

**CRÍTICO:** `content/raw/` DEVE estar no `.gitignore`. Não commitar transcrições.

## File List
- `aiox-academy/scripts/extract-transcripts.py` — novo
- `aiox-academy/requirements.txt` — novo
- `aiox-academy/content/raw/youtube/` — directório gerado (gitignored)

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-06-01 | 1.0.0 | Story criada a partir do PRD aprovado | @sm (River) |
