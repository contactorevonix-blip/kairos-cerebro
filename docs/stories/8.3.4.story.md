# Story 8.3.4: Skill Mapping & Validation

**Status:** Done
**Story ID:** 8.3.4
**Created:** 2026-06-11
**Epic:** EPIC-8 — Framework Evolution (Phase 3: Squad Creator)
**PRD Source:** `docs/prd/epic-8/phase-3-squad-creator.md#834-skill-mapping--validation-15sp`
**Story Points:** 1.5

---

## Story

**As a** framework operator,
**I want** a validation system that maps mentor skills to cloned squad and validates command chains,
**so that** I can identify which skills transferred cleanly and which need customization before using the squad.

---

## Acceptance Criteria

- [x] **AC1:** Registry `.aiox/squad-dnas/{squad-id}-skills.json` created with skill availability matrix (skill → command → task mapping)
- [x] **AC2:** Validation command `aiox squad validate {squad-id}` checks: all tasks exist, all command chains valid, dependencies installed
- [x] **AC3:** Report generated at `docs/squad-validations/{squad-id}-report.md` with: ✅ Passed skills, ⚠️ Requires customization, ❌ Missing dependencies
- [x] **AC4:** Report includes actionable steps for missing dependencies
- [x] **AC5:** Validation runs in <5 seconds for typical squad

---

## Dependencies

- **Depends on:** 8.3.3 (Squad Template Generation)
- **Blocks:** 8.3.8 (Integration Tests)

---

## Scope

**IN:**
- Squad.yaml parsing
- Task file existence checks
- Command chain validation
- Dependency resolution

**OUT:**
- Auto-installing missing dependencies (user action)
- Modifying command chains

---

## Risks

- **Risk:** Validation too strict (rejects valid squads) or too lenient (misses errors)
- **Mitigation:** Document validation rules clearly; examples in docs

---

## 🤖 CodeRabbit Integration

**Primary Type:** Feature (Validation + reporting)  
**Agents:** @dev (implementation), @qa (validation logic)  
**Max Iterations:** 2

---

## File List

- [x] `squads/squad-creator/core/skill-validator.js` — Main validation module (created)
- [x] `tests/squad-creator/skill-validator.test.js` — Unit tests (created, 17 tests PASS)
- [x] `bin/commands/squad-validate.js` — CLI command handler (created)

---

## Dev Agent Record

**Agent Model Used:** claude-opus-4-8 (Dex / @dev)
**Mode:** YOLO (autonomous)

### Completion Notes

- Skill matrix is derived from the mentor's `dependencies.tasks` (the reliably-parseable, authoritative skill set) rather than the `commands:` list-of-maps, because `voice-dna.parseAgentBlock` documents that it does NOT parse list-of-maps. Each task → derived command name → on-disk existence check classifies the skill into ✅ passed / ⚠️ requires_customization / ❌ missing_dependencies. When a caller supplies a fully-parsed agent (commands as an array), unmapped commands are enriched as ⚠️ entries.
- Reuses (IDS REUSE) `parseSimpleYaml` + `loadMentorAgent` from `squad-template-generator.js`; no new YAML runtime dependency.
- Art. IV (No Invention): skills, chains, and task existence are all real (mentor YAML + filesystem). No fabricated chains.
- Path-traversal hardened (`safeSquadId` via `path.basename` + slug regex) on every file write.
- Graceful degradation: unresolved mentor → empty matrix, never throws.

### Debug Log References

- `node --test tests/squad-creator/skill-validator.test.js` → 17/17 PASS.

---

## Change Log

- **2026-06-11 @sm:** Story created (Draft)
- **2026-06-11 @po:** Path re-target — main module `skill-validator.js` moved from `.aiox-core/core/squad-creator/` (L1, NEVER modify) to `squads/squad-creator/core/` (L4). Unblocks implementation against framework-boundary deny rules. No logic change. Status remains Ready.
- **2026-06-11 | 1.0.0 | Development started (YOLO mode) — Status: Ready → InProgress | @dev**
- **2026-06-11 | 1.0.0 | Implemented skill-validator + CLI + 17 tests (all AC met) — Status: InProgress → InReview | @dev**
| 2026-06-12 | 1.1.0 | QA Gate PASS — Status: InReview → Done | @qa |

## QA Results

### Review Date: 2026-06-12

### Reviewed By: Quinn (Test Architect)

Independently verified: 17/17 unit tests pass, eslint clean. The `squad validate` CLI was smoke-tested live — it ran in 8ms (well under the AC5 <5s budget), produced the skills-matrix JSON (`.aiox/squad-dnas/{id}-skills.json`, AC1) and the report MD (`docs/squad-validations/{id}-report.md`, AC3), and classified 22 skills as passed/0 customization/0 missing. Command set is correctly derived from the authoritative `dependencies.tasks` (the reliably-parseable source). Path-traversal hardening present (`safeSquadId`). All 5 ACs met.

### Gate Status

Gate: PASS → docs/qa/gates/8.3.4-skill-mapping-validation.yml
