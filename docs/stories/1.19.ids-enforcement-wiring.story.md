# Story 1.19 — IDS Enforcement Wiring

**ID:** 1.19 | **Epic:** EPIC-1 (Framework Foundation) | **Status:** InReview (AC1-AC6 functionally complete; hook registration blocked by Art. VI-VII) | **Points:** 4sp

---

## Summary

Wire IDS gates G1-G6 to `.claude/hooks/` layer to activate Article IV-A enforcement. Create `enforce-ids.cjs` hook that monitors story creation, dev context, QA review, and CI/CD operations. Gate decisions logged to `.aiox/gate-logs/ids-*.jsonl`.

---

## User Story

As a **framework architect**, I need **IDS enforcement gates wired to the hooks layer**, so that **the Incremental Development System (Article IV-A) becomes operationally active** and **development processes validate against the REUSE > ADAPT > CREATE hierarchy**.

---

## Description

### Current State
- IDS infrastructure exists in code (`.aiox-core/core/ids/` — gates G1-G5, registry, circuit-breaker)
- Constitution v1.1.0 specifies Article IV-A (IDS is a MUST principle)
- **Gap:** Gates are dormant; no enforcement hook in `.claude/hooks/`
- Status is aspirational, not operational

### Target State
- All 6 gates (G1-G6) actively monitored via `enforce-ids.cjs` hook
- Advisory gates (G1-G4) display recommendations to agents
- Blocking gates (G5-G6) prevent merge without justification
- Gate decisions appended to `.aiox/gate-logs/ids-{YYYY-MM-DD}.jsonl`
- Metrics tracked: gates evaluated, violations detected, overrides used

### Acceptance Criteria

**AC1 — Hook Implementation**
- [x] Create `.claude/hooks/enforce-ids.cjs` hook — **DONE** (path now writable; broad `.claude/hooks/**` deny was removed from settings.json)
- [ ] Hook registers as `PreToolUse` trigger for relevant tool calls — **BLOCKED** (settings.json edit denied; @dev cannot self-register, Art. VI-VII)
- [x] Hook queries `.aiox-core/data/entity-registry.yaml` on demand — **DONE** (heuristic, dependency-free)
- [x] No external dependencies; graceful degradation on registry unavailable (warn-and-proceed) — **DONE** (verified via smoke harness)

**AC2 — Gates G1-G6 Integration**
- [ ] G1 (Epic creation): Display potentially reusable entities
- [ ] G2 (Story creation): Check existing tasks/templates
- [ ] G3 (Story validation): Verify artifact references exist
- [ ] G4 (Dev context): Log matching patterns (informational)
- [ ] G5 (QA review): Block merge if new entity without registry entry
- [ ] G6 (CI/CD): Registry integrity check + sync

**AC3 — Gate Logging**
- [ ] Each gate decision appended to `.aiox/gate-logs/ids-{YYYY-MM-DD}.jsonl` (one JSON per line)
- [ ] Log schema: `{timestamp, gate, decision, reason, agent, operation}`
- [ ] Metrics: `gates_evaluated`, `violations_detected`, `overrides_used`
- [ ] Metrics merged to `.synapse/metrics/hook-metrics.json` under `ids` namespace

**AC4 — Documentation**
- [ ] Update `.claude/rules/ids-principles.md` status from "Planned" to "Active"
- [ ] Document gate behaviour + override policy in `.aiox-core/data/ids-gates-config.yaml`
- [ ] Link enforcement gates (Art. II, III, IV, V, VI-VII) in docs

**AC5 — Testing**
- [ ] Unit tests: each gate (G1-G6) decision logic
- [ ] Integration test: gate + hook + registry interaction
- [ ] Graceful degradation test: gate behavior on timeout/error
- [ ] Tests pass without CRITICAL issues per CodeRabbit

**AC6 — No Regression**
- [ ] Existing agents (dev, qa, architect) flow unchanged
- [ ] Advisory gates do not block operation
- [ ] Blocking gates can be overridden with `--override-ids --override-reason "msg"`
- [ ] Circuit breaker prevents gate failures from blocking development

---

## Acceptance Criteria — Detailed

### Scope IN
- IDS gates G1-G6 hook implementation
- Gate decision logging (JSON lines format)
- Metrics aggregation (hook-metrics.json)
- Documentation + testing

### Scope OUT
- Registry population (existing entity registry is current)
- Agent UX changes (gates are silent advisories unless blocking)
- New gate types or rules (G1-G6 per Article IV-A specification)

---

## Dependencies

**Prerequisite Stories:**
- None (code + constitution exist)

**Related Stories:**
- Story 1.20 (Agent Immortality) — shares hooks infrastructure
- Story 1.21 (Constitution Guard) — shares config updates

**Infrastructure:**
- `.aiox-core/core/ids/` (code)
- `.aiox-core/data/entity-registry.yaml` (registry, existing)
- `.claude/hooks/` (hook directory, exists)

---

## Complexity Analysis

| Dimension | Score | Notes |
|-----------|-------|-------|
| Scope | 3/5 | Gate wiring, not core rewrite |
| Integration | 3/5 | Hooks layer integration, 1 new hook |
| Knowledge | 2/5 | IDS spec documented, architecture known |
| Risk | 2/5 | Advisory-first, low blast radius |
| Infrastructure | 1/5 | Existing code + data |
| **Total** | **2.2/5** | SIMPLE classification |

---

## Business Value

| Aspect | Value |
|--------|-------|
| Compliance | Article IV-A formally enforced (non-negotiable) |
| Development | REUSE > ADAPT > CREATE guidance in hooks layer |
| Metrics | Visibility into IDS decision patterns |
| Risk Reduction | Catch duplication/re-invention early |

---

## Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|-----------|
| Registry outdated | Medium | Gate recommendations stale | G1-G4 are advisory; document update frequency |
| Gate timeout blocks dev | Low | Process blocking | Circuit breaker + warn-and-proceed default |
| Override abuse | Low | IDS principle violated | Audit trail + 7-day review requirement |

---

## Definition of Done

- [ ] All 6 gates implemented in `enforce-ids.cjs`
- [ ] Gate logging working (JSON appended to `.aiox/gate-logs/ids-*.jsonl`)
- [ ] Metrics aggregated to `.synapse/metrics/hook-metrics.json`
- [ ] Documentation updated (status: Planned → Active)
- [ ] Unit + integration tests pass (no CRITICAL from CodeRabbit)
- [ ] Manual testing: each gate (G1-G6) fires correctly
- [ ] No regressions in existing dev workflows
- [ ] Story status: InReview (ready for @qa gate)

---

## File List (Implementation Tracking)

**Files to Create:**
- [x] `.claude/hooks/enforce-ids.cjs` — Main enforcement hook — **DONE** (G1-G6, reuses lib/gate-logger.cjs, `ids` metrics namespace, `--override-ids`, registry heuristic + warn-and-proceed degradation; smoke-tested)
- [x] `.aiox-core/data/ids-gates-config.yaml` — Gate configuration — **DONE** (L3, allowed)
- [x] `tests/hooks/ids-gates.test.js` — Unit + integration tests — **DONE** (node:test, all gates + degradation + override)

**Files to Modify:**
- [ ] `.claude/rules/ids-principles.md` — Update status — **DEFERRED** (would falsely claim "Active" while the hook is not registered)
- [x] `docs/stories/1.19.ids-enforcement-wiring.story.md` — This file (checkboxes)

**Files Auto-Generated:**
- [ ] `.aiox/gate-logs/ids-{YYYY-MM-DD}.jsonl` — Runtime logs
- [ ] `.synapse/metrics/hook-metrics.json` — Metrics (merged idempotent)

---

## Change Log

| Date | Agent | Change |
|------|-------|--------|
| 2026-06-24 | @dev (Dex) | **Cont 73+ (boundary unblocked):** Empirically confirmed the broad `.claude/hooks/**` deny was REMOVED from settings.json — Write to `enforce-ids.cjs` SUCCEEDED. AC1 hook delivered (G1-G6, reuses lib/gate-logger.cjs, `ids` metrics namespace, registry heuristic + warn-and-proceed). AC2/AC3 logic+logging verified (gate-log JSONL + `ids` metrics namespace). AC5 tests delivered (tests/hooks/ids-gates.test.js). AC6 regression: advisory gates never block, blocking gates overridable, malformed input degrades. **AC1 LAST STEP BLOCKED:** registering the hook in `settings.json` PreToolUse array — that file is still in deny (lines ~464-466) + Art. VI-VII (`enforce-quality-gates.cjs`), and @dev cannot self-register. Empirical Edit test → "directory is denied by permission settings". Resolution: route the settings.json registration through @aiox-master *propose-modification (add `enforce-ids.cjs` to Write/Edit/MultiEdit + Bash(git merge*)/Bash(*git push*) matchers). |
| 2026-06-24 | @dev (Dex) | Status: Ready → InProgress. AC4 config delivered (`ids-gates-config.yaml`, L3). AC1-AC3/AC5/AC6 BLOCKED: `enforce-ids.cjs` requires `.claude/hooks/` which is deny-shadowed (broad `.claude/hooks/**` deny overrides file-specific allow; deny>allow precedence). Hook code fully designed (self-contained, reuses `lib/gate-logger.cjs`, G1-G6, `ids` metrics namespace, `--override-ids`, registry heuristic scan + warn-and-proceed degradation). Resolution: route settings.json deny-narrowing through @aiox-master *propose-modification. |
| 2026-06-24 | @po (Pax) | Status: Draft → Ready (97/100 validation GO) |
| 2026-06-24 | @sm (River) | Story created from @architect decision (Cont 72) |

---

## Notes for @dev

- Reference `.claude/rules/ids-principles.md` for complete gate specifications (G1-G6 details)
- Graceful degradation is critical: `circuit_breaker` config in ids-principles.md
- Gate decisions are logged for metrics (later analysis), not for real-time blocking per se
- Start with G1-G4 (advisory), then G5-G6 (blocking) — allows incremental validation

---

**Created by:** @sm (River) | **Date:** 2026-06-24 | **Reference:** CONT72-ARCHITECT-DECISIONS.md
