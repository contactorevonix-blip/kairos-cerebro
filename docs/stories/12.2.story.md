---
storyId: "12.2"
epic: EPIC-12
title: "Validate @devops-Exclusive git push/PR Authority (FR-2.1–2.4)"
status: Done
effort: "1.5 days (Medium, complexity 5/5)"
priority: P0
wave: "Wave 1"
owner: "@dev"
createdBy: "@sm (River)"
createdDate: "2026-06-20"
---

# Story 12.2 — Agent Authority Validation (Art. II)

## Description

Validate @devops-Exclusive git push/PR Authority (FR-2.1–2.4). Confirm and
strengthen the active-agent resolution chain used by the Art. II gate, ensure
non-@devops push/PR attempts are blocked across all detection paths, and keep
override and metrics behavior correct.

## Acceptance Criteria

- [x] enforce-agent-authority.cjs resolves active agent (4 paths: env vars, inline scope, session state, fallback)
- [x] Non-@devops push/PR attempts blocked
- [x] Agent detection order: AIOX_ACTIVE_AGENT → AIOX_AGENT → hook-metrics.json session → default-DENY
- [x] Tests cover all 4 detection paths + override scenario (--skip-devops-check)
- [x] Metrics updated in .synapse/metrics/hook-metrics.json

## Dependencies

- Story 12.1
- EPIC-9 gate implementations
- entity-registry.yaml

## Wave / Barrier Gate

- **Wave:** Wave 1 — Framework Boundary & Agent Authority (Stories 12.1–12.2)
- **Gate condition:** All Phase 1 stories (12.1–12.2) PASS QA before Wave 2 starts
- **Barrier Gate 1 — Exit criteria:** 12.1 PASS ✅, 12.2 PASS ✅

## Files

- Modify: `.claude/hooks/enforce-agent-authority.cjs`
- Modify: `.aiox-core/data/entity-registry.yaml` (populate task→agent mappings) — NOTE (@po): actual path is `.aiox-core/data/entity-registry.yaml` (L3 Config, mutable); there is no `entity-registry.yaml` at repo root. @dev must edit the L3 file, not create a root duplicate.
- Create: `tests/hooks/test-agent-authority-detection.test.js`

## File List (to be populated during @dev implementation)

**Modified:**
- `.claude/hooks/lib/gate-logger.cjs` — added structured `resolveActiveAgent(command, cwd)` returning `{ agent, source }` with 4-path detection (env → inline → session → default-DENY); `getActiveAgent` kept as retro-compatible string wrapper; exported `resolveActiveAgent`.
- `.claude/hooks/enforce-agent-authority.cjs` — Art. II gate now uses `resolveActiveAgent` and logs `detectionSource` on every decision (allow / override / block).
- `.aiox-core/data/entity-registry.yaml` — added `exclusiveOperations` (git push, git push --force, gh pr create, gh pr merge) to the `@devops` agent entity (advisory metadata, additive).

**Created:**
- `tests/hooks/test-agent-authority-detection.test.js` — 19 tests covering the 4 detection paths, precedence order, override scenario, retro-compatibility, and metrics/detectionSource logging.

## Dev Notes

- **IDS decisions:** gate-logger / enforce-agent-authority / entity-registry → ADAPT (retro-compatible, additive); test file → CREATE (no prior coverage of structured detection paths).
- **Retro-compatibility:** `getActiveAgent` wrapper preserved so all EPIC-9 callers/tests (21 tests) keep working. Full hook suite: 180 → 199 tests, 0 fail.
- **`detectionSource` JSONL field:** additive only — existing log parsers (timestamp/article/gate/decision/agent/operation) are unaffected.
- **Fail-safe:** `default-DENY` resolves to empty agent, which `isDevOpsAgent('')` rejects, so an unresolved agent can never pass an Art. II push/PR.

## Change Log

- 2026-06-20 @sm: Story created from EXECUTION-PLAN.yaml (Cont 63)
- 2026-06-20 @po (Pax): Validated GO (8/10) — Status: Draft → Ready. Anti-hallucination PASS (enforce-agent-authority.cjs, hook-metrics.json verified; FR-2.1–2.4 confirmed in requirements.json). Should-fix applied: corrected entity-registry.yaml path to `.aiox-core/data/` (was ambiguous root path). Barrier Gate 1 candidate.
- 2026-06-20 @dev (Dex): Implementation complete. Status: Ready → InProgress → InReview. All 5 ACs met. Hardened active-agent resolution into structured 4-path `resolveActiveAgent` ({ agent, source }) with retro-compatible `getActiveAgent` wrapper; added `detectionSource` logging to Art. II gate JSONL; added `exclusiveOperations` to @devops in entity-registry; created 19-test suite. Quality gates: lint clean, typecheck clean, 199/199 hook tests PASS (incl. 21 EPIC-9 regression). Ready for @qa.
- 2026-06-20 @dev (Dex): CodeRabbit review (uncommitted, --agent). 2 in-scope findings on the new test file (trivial type assertion + temp-dir cleanup) — both FIXED. 1 CRITICAL + remaining findings are OUT OF SCOPE: they target gitignored runtime artifacts (`.aiox/gate-logs/*.jsonl` log lines from other stories' activity, `.aiox/task-logs/*.json`) — not files authored by this story; left untouched per scope constraints. Re-ran gates after fixes: lint clean, typecheck clean, 199/199 PASS.
- 2026-06-20 @qa (Quinn): QA Gate **PASS** — Status: InReview → Done. All 5 ACs verified against real code/tests/runtime. 199/199 hook tests PASS (19 new 12.2 + 21 EPIC-9 regression + 23 enforcement gates). Lint clean, typecheck clean. Live gate-log confirms `detectionSource` is additive (pre-12.2 lines lack it, post-12.2 lines carry it — parsers unaffected). `@devops` entity in entity-registry has all 4 exclusiveOperations. Enforcement metrics live. Gate: `docs/qa/gates/12.2-agent-authority-validation.yml`. Barrier Gate 1 (Wave 1) clears: 12.1 PASS + 12.2 PASS.

## QA Results

### Review Date: 2026-06-20

### Reviewed By: Quinn (Test Architect)

### Verdict: PASS

### 7-Point Quality Checklist

| # | Check | Result | Notes |
|---|-------|--------|-------|
| 1 | Code Review | PASS | Clean ADAPT: `resolveActiveAgent(command, cwd)` new, returns structured `{ agent, source }`; `getActiveAgent` kept as retro-compat string wrapper delegating to it. `detectionSource` logged on every decision (allow/override/block). 4-path order is explicit and commented. |
| 2 | Unit Tests | PASS | 19 new tests covering all 4 paths, precedence (env>inline>session, AIOX_ACTIVE_AGENT>AIOX_AGENT), override, retro-compat, metrics + detectionSource logging. Full suite **199/199 PASS, 0 fail**. |
| 3 | Acceptance Criteria | PASS | 5/5 verified against real artifacts (see AC table below). |
| 4 | No Regressions | PASS | 21 EPIC-9 `enforce-agent-authority` tests PASS + 23 enforcement-gate tests PASS. Suite grew 180->199 (additive). Live log proves `detectionSource` is parser-safe: pre-12.2 lines omit it, post-12.2 lines carry it. |
| 5 | Performance | PASS | Detection = in-process env reads + a single JSON read only on the session path. Negligible overhead; gate remains fast. |
| 6 | Security | PASS | `default-DENY` fail-safe (empty agent → `isDevOpsAgent('')` = false → cannot pass Art. II). Block emits deny + exit 2. Override `--skip-devops-check` works as designed and is audit-logged. |
| 7 | Documentation | PASS | File List accurate (3 modified, 1 created), Change Log complete, story refs FR-2.1–2.4. |

### Acceptance Criteria Verification

| AC | Status | Evidence |
|----|--------|----------|
| AC1 — resolves via 4 paths (env, inline, session, fallback) | PASS | `gate-logger.cjs:88-111`; 5 path tests + structure-invariant test |
| AC2 — non-@devops push/PR blocked | PASS | `enforce-agent-authority.cjs:104-116` (deny + exit 2); live log `agent:dev decision:block` |
| AC3 — order AIOX_ACTIVE_AGENT → AIOX_AGENT → session → default-DENY | PASS | `gate-logger.cjs:90-110`; 4 precedence tests |
| AC4 — tests cover 4 paths + override (--skip-devops-check) | PASS | 19-test suite; 3 override tests; live log `decision:override` audit entry |
| AC5 — metrics updated in hook-metrics.json | PASS | enforcement counters live (gatesEnforced 86, violationsDetected 48, violationsBlocked 24, overridesUsed 24); `enforcementUpdatedAt` today |

### Evidence Spot-Checks (anti-false-positive)

- **entity-registry.yaml** (`.aiox-core/data/`, L3): `@devops` entity (line 14729) carries `exclusiveOperations: [git push, git push --force, gh pr create, gh pr merge]` — matches gate `REMOTE_OPERATION_PATTERNS` exactly.
- **Live runtime gate-log** (`art-ii-agent-authority-2026-06-20.jsonl`): lines 1–21 (pre-12.2) have NO `detectionSource`; lines 22–37 (post-12.2) carry `detectionSource:"env"` — proves the field is additive and existing parsers are unaffected. The gate also fired on this very QA review (it intercepted a literal flagged command in a shell heredoc) — live confirmation Art. II enforcement is active.
- **Lint** clean, **typecheck** clean.

### Barrier Gate 1 (Wave 1)

12.1 PASS + 12.2 PASS → **Wave 1 (Framework Boundary & Agent Authority) exit criteria met. Wave 2 may start.**

### Gate Status

Gate: PASS → docs/qa/gates/12.2-agent-authority-validation.yml

### Recommended Status

**Done** — ready for @devops push.
