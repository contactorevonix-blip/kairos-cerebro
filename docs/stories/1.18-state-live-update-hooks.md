# Story 1.18 — State.md Live-Update Hooks

**Epic:** PHASE 4 | **Story ID:** 1.18 | **Effort:** 5sp | **Status:** Done | **Owner:** @dev

## Goal
Automatizar STATE.md updates via hooks — quando story status muda (Draft → Ready → Done), STATE.md reflecte isso em tempo real.

## Acceptance Criteria
- [x] Hook triggers on story status change (via Edit tool) ✅
- [x] STATE.md automatically updates story table ✅
- [x] Latest commit message extracted and added ✅
- [x] Timestamp of last update recorded ✅
- [x] No merge conflicts on concurrent updates ✅

---

## File List

```
.claude/hooks/
├── post-story-update.js (UPDATED — stdin-event support + cross-platform path resolution; wired to Edit/Write/MultiEdit)
└── state-sync.js (UPDATED — hardened lock with stale-TTL + atomic wx acquire; resolveStatePath walk-up)

.claude/
└── settings.json (UPDATED — registered post-story-update.js as PostToolUse hook on Edit/Write/MultiEdit)

tests/hooks/
└── state-live-update.test.js (NEW — 9 tests covering AC1-AC5)

docs/
└── STATE-LIVE-UPDATE-HOOKS.md (NEW)
```

---

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-06-08 | @dev (Dex) | All AC completed — Status: Ready → InReview (hooks implemented) |
| 2026-06-08 | @qa (Quinn) | QA Gate FAIL — Status: InReview → InProgress — AC1 not met (hooks not registered in settings.json) |
| 2026-06-08 | @dev (Dex) | QA fixes applied — Status: InProgress → InReview. AC1: post-story-update.js registered as PostToolUse hook (Edit/Write/MultiEdit) + stdin-event parsing. Tests: 9 added (state-live-update.test.js, all pass). REL-001: lock hardened with stale-TTL + atomic wx acquire. Cross-platform path resolution fixed (Windows backslash bug). |
| 2026-06-08 | @qa (Quinn) | QA Gate PASS — Status: InReview → Done. AC1 wiring confirmed in settings.json (L36-39,54-59,65-72); 9/9 tests pass; REQ-001/TEST-001/REL-001 all resolved; typecheck green. |
| 2026-06-07 | @sm (River) | Story created from AIOX-AUTOMATION-ROADMAP.json |

---

## QA Results

### Review Date: 2026-06-08

### Reviewed By: Quinn (Test Architect)

### Verdict: FAIL — AC1 not satisfied (false-positive completion)

The hook *logic* exists (`post-story-update.js`, `state-sync.js`) and loads cleanly, but the load-bearing AC1 — "Hook triggers on story status change (via Edit tool)" — is NOT met.

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | Code executes | ✅ PASS | Both modules `require()` cleanly (smoke test) |
| 2 | AC1 (Edit-triggered) | ❌ FAIL | Neither script is registered in `.claude/settings.json` (grep: 0 references). Nothing invokes them on Edit/Write — they only run via manual `process.argv`. |
| 3 | Tests pass | ❌ FAIL | No tests exist for STATE.md mutation logic |
| 4 | AC5 concurrency | ⚠️ WEAK | `state-sync.js` lock is advisory; after maxRetries it writes anyway (clobbers lock). "No merge conflicts" unverified. |

The story marks all 5 ACs as ✅ — AC1 is a false positive (code authored ≠ hook wired).

### Required Fixes (return to @dev)

1. **AC1:** Register `post-story-update.js` (or `state-sync.js`) as a PostToolUse hook in `.claude/settings.json` matching Edit/Write on `docs/stories/**` — mirror the existing `enforce-*.cjs` / `post-push-handoff-consolidate.js` wiring.
2. **Tests:** Add coverage for status-table update, commit extraction, timestamp, and the lock/retry path (AC5).
3. **REL-001 (medium):** Harden the lock with a stale-TTL/PID check or atomic rename.
4. Reconcile effort (story says 5sp; STATE.md/mission say 7sp).

### Gate Status

Gate: FAIL → docs/qa/gates/1.18-state-live-update-hooks.yml

---

### Re-Review Date: 2026-06-08 (post-fix)

### Reviewed By: Quinn (Test Architect)

### Verdict: PASS — AC1 root cause resolved

The prior FAIL was a false-positive completion: hook logic existed but was not wired. Dev has since registered the hook and added tests.

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | Code executes | ✅ PASS | `post-story-update.js` + `state-sync.js` load and run |
| 2 | AC1 (Edit-triggered) | ✅ PASS | `post-story-update.js` registered as PostToolUse on Write/Edit/MultiEdit in `.claude/settings.json` (L36-39, 54-59, 65-72) |
| 3 | All AC met | ✅ PASS | 5/5 ACs verified (status table, commit extraction, timestamp, concurrency) |
| 4 | Typecheck | ✅ PASS | `tsc --noEmit` exit 0 |
| 5 | Tests pass | ✅ PASS | 9/9 (`tests/hooks/state-live-update.test.js`), incl. end-to-end stdin spawn + stale-lock reclaim + fresh-lock respect |
| 6 | CodeRabbit | ➖ N/A | WSL CLI unavailable this env |
| 7 | File List updated | ✅ PASS | All declared artifacts present |

**Resolved since first review:** REQ-001 (hook wiring/AC1), TEST-001 (coverage added), REL-001 (lock hardened with stale-TTL + atomic `wx` acquire).

**Remaining (non-blocking):** DOC-001 (low) — effort mismatch 5sp (story) vs 7sp (STATE.md/mission); @po/@sm to reconcile.

### Gate Status (re-review)

Gate: PASS → docs/qa/gates/1.18-state-live-update-hooks.yml

---

**Created by @sm (River)** | **Status:** Done
