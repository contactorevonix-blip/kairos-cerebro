# ============================================
# Synkra AIOX Environment Configuration
# ============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env with real credentials
# ============================================

# --------------------------------------------
# LLM Providers
# --------------------------------------------

# DeepSeek API (for claude-free command)
# Get your key at: https://platform.deepseek.com/api_keys
# Cost: ~$0.14/M tokens with tool calling support
DEEPSEEK_API_KEY=

# OpenRouter API (for multi-model routing)
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Anthropic API (direct, if not using Claude Max subscription)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OpenAI API Key - Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# --------------------------------------------
# Search & Research Tools
# --------------------------------------------

# Exa Search API (web search for agents)
# Get your key at: https://exa.ai/
EXA_API_KEY=

# Context7 (library documentation lookup)
# Usually free, no key required for basic usage
CONTEXT7_API_KEY=

# --------------------------------------------
# Database & Backend
# --------------------------------------------

# Supabase (database, auth, storage)
# Get from your Supabase project settings
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --------------------------------------------
# Version Control & CI/CD
# --------------------------------------------

# GitHub Token (for GitHub CLI and API access)
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=

# --------------------------------------------
# Project Management
# --------------------------------------------

# ClickUp API (if using ClickUp integration)
# Get from: ClickUp Settings > Apps > API Token
CLICKUP_API_KEY=

# --------------------------------------------
# Automation & Workflows
# --------------------------------------------

# N8N (workflow automation)
# From your N8N instance settings
N8N_API_KEY=
N8N_WEBHOOK_URL=

# --------------------------------------------
# Monitoring & Analytics
# --------------------------------------------

# Sentry (error tracking)
SENTRY_DSN=

# --------------------------------------------
# Cloud Providers
# --------------------------------------------

# Railway (deployment)
RAILWAY_TOKEN=

# Vercel (deployment)
VERCEL_TOKEN=

# --------------------------------------------
# AIOX Core Configuration
# --------------------------------------------
NODE_ENV=development
AIOX_VERSION=5.2.9

# ============================================
# KAIROS CHECK — Production Variables
# ============================================

# --------------------------------------------
# Server
# --------------------------------------------
PORT=8080
NODE_ENV=production                        # production | development

# --------------------------------------------
# Admin Authentication
# --------------------------------------------
# Bearer token para /dashboard e /api/stats/*
# Gerar: node -e "require('crypto').randomBytes(32).toString('hex')" | tr -d '\n'
# Mínimo 32 chars. Railway faz process.exit(1) se vazio em production.
KAIROS_ADMIN_TOKEN=

# --------------------------------------------
# Vault (AES-256-GCM)
# --------------------------------------------
# Passphrase para encriptar dados sensíveis em vault.json
# Mínimo 12 chars. O servidor inicializa o vault automaticamente no startup.
KAIROS_MASTER_PASSPHRASE=

# --------------------------------------------
# Data Directory
# --------------------------------------------
# Caminho do volume Railway. Default: /app/.kairos-data (produção)
# Localmente: .kairos-data (relativo ao working dir)
KAIROS_DB_DIR=/app/.kairos-data

# --------------------------------------------
# Reputation Graph Adapter
# --------------------------------------------
# json  — ficheiros on-disk (default local, sem deps)
# redis — Redis (cross-tenant intelligence, recomendado produção)
KAIROS_RG_ADAPTER=json
# URL do Redis Railway (necessário se KAIROS_RG_ADAPTER=redis)
KAIROS_REDIS_URL=redis://default:<password>@<host>:<port>

# --------------------------------------------
# Cloudflare R2 Backup
# --------------------------------------------
# Bucket name (criar em cloudflare.com → R2 → Create bucket)
R2_BACKUP_BUCKET=kairos-backup
# Endpoint: https://<account_id>.r2.cloudflarestorage.com
# Encontrar em: Cloudflare → R2 → bucket → Settings
R2_ENDPOINT=
# API Token com "Object Read & Write" no bucket kairos-backup
# Criar em: Cloudflare → R2 → Manage R2 API Tokens
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
# Agendar (Railway Cron): 0 2 * * * node bin/backup-volume.js
# Ou manual: npm run backup

# --------------------------------------------
# Custom Configuration
# --------------------------------------------
# Add your custom API keys below

# === AIOX Variables (added 2026-05-28) ===

# AIOX_NOTE: The following variables already exist with different values:
# AIOX_SUGGESTED: NODE_ENV=development

# === AIOX Variables (added 2026-06-01) ===

# AIOX_NOTE: The following variables already exist with different values:
# AIOX_SUGGESTED: NODE_ENV=development
