# ═══════════════════════════════════════════════════════════════════════════════
# DEPRECATED STUB — squad.yaml is no longer the source of truth.
# ═══════════════════════════════════════════════════════════════════════════════
# SQUAD-FUSION.6 (AC3, Pedro's amendment 2026-07-01): squad.yaml and config.yaml were
# the SAME concept → consolidated into a single source of truth: config.yaml.
# Every field of the former squad.yaml was mapped into config.yaml; the mapping is
# recorded field-by-field in docs/qa/squad-fusion/collision-resolution-log.md.
#
# This file is kept (not deleted) as a thin stub so any external reference to squad.yaml
# does not break, and to keep the consolidation reversible. It carries NO authoritative
# config. Canonical structural check SC_STRUCT_001 requires config.yaml (NOT squad.yaml);
# a future cleanup story may remove this stub.
#
# → Read squads/squad-creator/config.yaml for the real configuration.

deprecated: true
superseded_by: squads/squad-creator/config.yaml
consolidated_on: "2026-07-01"
mapping_log: docs/qa/squad-fusion/collision-resolution-log.md
